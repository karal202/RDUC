export default function ValidationModal({ form, setForm, onSubmit, onClose }) {
  return <div className="modal-overlay" onClick={onClose}><div className="modal-box" onClick={(e) => e.stopPropagation()}><div className="modal-header"><h3>Test API Validate Key</h3></div><form onSubmit={onSubmit} className="form-grid"><label className="form-group">License Key<input value={form.key_code} onChange={(e) => setForm({ ...form, key_code: e.target.value })} /></label><label className="form-group">HWID Thiết bị<input value={form.device_hash} onChange={(e) => setForm({ ...form, device_hash: e.target.value })} /></label><div className="modal-actions"><button type="button" className="btn-secondary" onClick={onClose}>Hủy</button><button type="submit" className="btn-primary">Gửi kiểm tra</button></div></form></div></div>;
}
